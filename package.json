{
  "name": "@pinax/token-api",
  "version": "0.1.2",
  "description": "Pinax Token API - Power your apps & AI agents with real-time token data",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "bin": {
    "@pinax/token-api": "./dist/bin/cli.js",
    "token-api": "./dist/bin/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "src",
    "bin"
  ],
  "scripts": {
    "prepublishOnly": "bun run build",
    "generate": "openapi-typescript openapi.json -o src/openapi.d.ts",
    "build": "bun run generate && bun build src/index.ts --outdir dist --target node && bun build bin/cli.ts --outdir dist/bin --target node && bun run build:types",
    "build:types": "tsc --emitDeclarationOnly --declaration --outDir dist && cp ./src/openapi.d.ts dist/openapi.d.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "cli": "bun run ./bin/cli.ts",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "biome format --write ."
  },
  "keywords": [
    "pinax",
    "token-api",
    "blockchain",
    "evm",
    "ethereum",
    "sdk"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pinax-network/token-api-sdk.git"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.6",
    "@types/bun": "latest",
    "openapi-typescript": "^7.0.0",
    "typescript": "^5"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "dotenv": "^17.2.3",
    "openapi-fetch": "^0.13.0",
    "p-queue": "^9.0.1"
  }
}
